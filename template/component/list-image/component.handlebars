<!--<div class="component component-image">
    <div class="hd">
        <h3>{{config.title}}</h3>
    </div>
    <div class="bd">
        <ol class="component-image-list">
          {{#each data}}
            <li class="component-image-item">
              <a href="{{pageUrl}}" class="component-image-thumb">
                <img src="{{imgPath}}" alt="" height="{{../config.imageHeight}}" width="{{../config.imageWidth}}">
              </a>
              <p class="component-image-title">{{{transformTitle ../config}}}</p>
            </li>
          {{/each}}
        </ol>
    </div>
    {{#if config.showMore}} 
        <div class="showmore"><a href="{{config.link}}">More</a></div>
    {{/if}}
</div>-->